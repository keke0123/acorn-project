<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="imageupload">	
	<insert id="insert" parameterType="imageUploadDto">
		INSERT INTO acorn_board
		(num_board, id_writer, tag, orgfilename, fileSize, caption, regdate)
		VALUES(acorn_board_seq.NEXTVAL, #{id_writer}, #{tag},
		#{orgfilename}, #{fileSize}, #{caption}, SYSDATE)
	</insert>
</mapper>























